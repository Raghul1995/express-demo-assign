apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-app-chart.fullname" . }}
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: LoadBalancer
