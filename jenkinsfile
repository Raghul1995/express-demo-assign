pipeline {
    agent any
    


    stages {
        stage('Build') {
            steps {
                echo "building the pipeline"
            }
        }

        stage('Deploy') {
            steps {
                echo "deploying the pipeline in the jenkins"
            }
        }
    }


}
